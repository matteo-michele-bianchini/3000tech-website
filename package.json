{
  "name": "3000tech",
  "version": "1.0.0",
  "description": "Landing page Impresa Artigiana Informatica",
  "scripts": {
    "dev": "live-server --port=5173 --host=0.0.0.0 --watch=.",
    "start": "live-server --port=5173 --host=0.0.0.0",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "playwright": "^1.59.1"
  }
}
